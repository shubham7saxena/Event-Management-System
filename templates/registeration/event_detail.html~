{% extends "registeration/base.html" %}

{% block body_block %}

<h2>{{ event.name }}</h2>
<div class="hero-unit">
	<i>{{ event.event_description }}</i>
				<p> First Time Preference:{{event.first_time_preference}}</p>
				<p> Actual Timing:{{event.Actual_time}}</p>
	<br>
	<a href="{% url 'events_event_user_registration' event_id=event.pk %}"> Register for {{ event.name}} </a>
</div>
{% endblock %}
