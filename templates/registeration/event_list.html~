{% extends "registeration/base.html" %}

{% block body_block %}

<h2>List of Events:</h2>
<div class="hero-unit">
	<form>
	{% for event in events%}
		<div>
			<a href="{% url "events_event_detail" event.pk %}">{{ event.name }}</a>
			<p> Contact: {{ event.event_coordi }} ({{ event.contact_id_coordinator}}) </p>
			{% if event.participants %}

				<p>{{event.participants}}</p>

				<p> First Time Preference:{{event.first_time_preference}}</p>
				<p> Actual Timing:{{event.Actual_time}}</p>

				{% for field in participant_list %}
				<p> {{ field.website }} </p>
				{% endfor %}
			{% endif %}
		</div>
		<br>
	{% endfor %}
	</form>
</div>

{% endblock %}
